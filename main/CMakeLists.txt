# main component intentionally left empty to avoid duplicate-target builds for the esp-idf PlatformIO environment
# The minimal bare blink is built via `bare_blink.c` in `src/` for the `esp32-c3-bare` env

